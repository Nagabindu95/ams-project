{
  "name": "class-project",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "npm install && cd app_public && npm install && npm run build && cd ..",
    "start": "node ./bin/www"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "^4.19.2",
    "http-errors": "~1.6.3",
    "jade": "^0.29.0",
    "mongoose": "^8.7.1",
    "morgan": "~1.9.1"
  }
}
